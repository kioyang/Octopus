class Project {

    name = 'project';

    description = '所有项目的父类';

    // 初始化项目
    init = (options) => {
        console.log(options);
    } 

    // 更新项目
    update  = (options) => {
        console.log(options);
    }

    // 提交项目

    // 部署项目
}